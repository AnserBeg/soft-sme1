# API Configuration
# VITE_BACKEND_URL is optional; if unset we fall back to the hard-coded /api on the same origin
VITE_BACKEND_URL=https://api.phoenixtrailers.ca
VITE_API_BASE_URL=https://api.phoenixtrailers.ca

# Development settings
VITE_DEV_MODE=true

